\documentclass{article}
\usepackage{spconf,amsmath,graphicx,booktabs,multirow,microtype,url,balance}
\usepackage[table]{xcolor}
% second-best entry of a column; a blue that still reads as a distinct tone in greyscale print
\definecolor{secondbest}{HTML}{1F4E9C}\newcommand{\second}[1]{\textcolor{secondbest}{#1}}
\input{numbers.tex}
% A rows file that ends in \\ cannot be followed by \bottomrule when read with \input
% (the row's look-ahead crosses the file boundary); the primitive reader has no such problem.
\makeatletter\newcommand{\inputrows}[1]{\@@input #1 }\makeatother

% Float placement: full-width floats otherwise drift past the technical pages.
\setcounter{topnumber}{3}\setcounter{bottomnumber}{2}\setcounter{totalnumber}{4}
\setcounter{dbltopnumber}{3}
\setlength{\abovecaptionskip}{2pt}\setlength{\belowcaptionskip}{1pt}
\setlength{\floatsep}{6pt plus 1pt minus 2pt}\setlength{\textfloatsep}{6pt plus 1pt minus 3pt}\setlength{\dblfloatsep}{6pt plus 1pt minus 2pt}\setlength{\dbltextfloatsep}{6pt plus 1pt minus 3pt}
\renewcommand{\topfraction}{0.92}\renewcommand{\bottomfraction}{0.7}
\renewcommand{\textfraction}{0.04}\renewcommand{\floatpagefraction}{0.72}
\renewcommand{\dbltopfraction}{0.92}\renewcommand{\dblfloatpagefraction}{0.72}

\title{SCENE-LEVEL TYPE-CONSISTENT RELABELLING FOR TRANSPARENT GLASS DETECTION}
\name{Heunseung Lim$^{*\dagger}$, Jihyeon Ryu$^{\dagger}$, Jinhoi Kim, Youngmin Son, Seongwoong Kim, and Gichul Yoo$^{*}$%
\thanks{$^{\dagger}$Equal contribution. $^{*}$Corresponding authors: Heunseung Lim and Gichul Yoo.}}
\address{SK intellix}

\begin{document}
\ninept
\maketitle

\begin{abstract}
GlassNICOL, a transparent-glass detection dataset, types each glass automatically from a depth-derived height in every view. Type is constant, so adjacent views should agree; the released training labels often disagree, while the hand-labelled test splits almost never do. We repair the labels with the structure that produced them. A scene is static, so every view of a glass is a view of one point on the table: we register views through the table plane, carry each box by its base point, chain the views of each glass, and give every box in a chain the chain's most frequent type. Detectors trained on the relabelled set, five (accelerator, seed) cells under a sealed protocol, gain out of distribution in type accuracy and six-class average precision over raw labels and over an adjacent-frame vote; on the standard split six-class average precision does not improve and recall falls about two points. Only type ids change.
\end{abstract}

\begin{keywords}
transparent object detection, label noise, multi-view consistency, relabelling, glass detection
\end{keywords}

\section{Introduction}
\label{sec:intro}
Transparent objects are expensive to label: depth sensors return little through glass and boundaries are weak, so ground truth comes from a proxy the sensor can see. ClearGrasp swaps each object for a spray-painted copy~\cite{sajjan2020cleargrasp}; KeyPose labels keypoints in a few views and propagates them through known camera poses, using an opaque twin only for depth~\cite{liu2020keypose}; GlassNICOL photographs each tabletop scene in 25 views, records it again with the glasses capped to recover depth, and types each box by matching its depth-derived height to the nearest known glass~\cite{gajdosech2025glassnicol}. KeyPose checks its geometry across views; nothing checks that the type assigned in one view agrees with the next.

We check. After registering adjacent training views and matching boxes, \FlipRateReg\% of matched box pairs that are the same physical glass carry a different type; the hand-labelled test splits pass the same test at \ManStdFlipRate\% and \ManOODFlipRate\% (Section~\ref{sec:noise}).

The structure that produced the labels can also repair them, and the scene says how. A GlassNICOL scene is static: the glasses stand on the table and only the head camera moves. Every view of a glass is therefore a view of one point of the table plane, its base, and a homography of that plane carries points of it exactly from one view to another. We register views pairwise through the table plane, carry each box by its base point, link boxes whose base points coincide, and merge the links into one chain per glass across the scene; each chain gives its most frequent type to every box in it (Fig.~\ref{fig:flow}, Section~\ref{sec:method}). Our earlier baseline linked only adjacent frames by box overlap, and such chains break at a missed frame; registering through the table plane and carrying base points over two hops raises the median votes per box from \TrkMedianVotes{} to \SceneMedianLen{} while merging no two glasses of different type on the manual splits. \SceneChangedPct\% of type ids change and no box moves.

Contributions: (i) a measurement of type-label inconsistency in a released benchmark against its own manual test labels; (ii) a scene-level relabelling method that uses only the dataset's own views, associates through the support plane without camera poses or depth, and changes type ids alone; (iii) evidence over five (accelerator, seed) cells on two accelerators under a protocol fixed before training (Section~\ref{sec:results}).

% spconf.sty does not define \paragraph, so run-in headings use \noindent\textbf.
\noindent\textbf{Relation to prior work.} Repeat-factor sampling~\cite{gupta2019lvis} and data pruning~\cite{paul2021datadiet,sorscher2022pruning} choose which images the network sees and take every label as given; relabelling changes what a label says. Label noise is usually found from model confidence~\cite{northcutt2021confident,northcutt2021errors} or the training loss~\cite{schubert2024labelerrors}, or absorbed by robust training~\cite{song2023noisysurvey}, in detectors too~\cite{liu2022nlte}; we locate it from the capture geometry, without a model. Tubelet methods smooth a detector's own output at test time, by re-scoring~\cite{kang2016tcnn,han2016seqnms} or propagation~\cite{tripathi2016propagation} and, in one case, a majority over a pseudo-tracklet~\cite{chen2016pseudotracklet}; offboard auto-labelling turns detections over a sequence into training labels~\cite{qi2021offboard}. We vote on released labels, not on detections. Ground-plane homographies associate people across cameras by their feet~\cite{khan2006homography}; we use the table plane the same way, across views of one moving camera. Label Fusion reprojects one 3D label into every frame~\cite{marion2018labelfusion}; GlassNICOL types each view separately, which is where the disagreement enters. KeyPose discards scans whose keypoints disagree~\cite{liu2020keypose}; we keep the frame and correct the label.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/figure_overview.png}
\caption{Overview. Views of a static scene are registered through the table plane, each box is carried by its base point and linked to the box it lands on, union-find merges the links into one chain per glass, and the chain's most frequent type replaces every type id in it. Photographs: views 6, 8 and 10 of one training scene with released boxes and types; inliers of the view 6 to 8 homography as displacement vectors; base points of view 6 carried into view 8 (open) and linked to its own (filled); view 6 with one colour per chain and a dashed box where the vote replaced the type; a scene-vote detector on a manual OOD test frame, manual boxes dashed, hand-picked where every manual box is detected with the correct type (Fig.~\ref{fig:qual} chooses its frames by rule). Overlays are real; stage symbols are schematic.}
\label{fig:flow}
\end{figure*}

\section{Proposed method}
\label{sec:method}
\subsection{Type labels disagree on the same object}
\label{sec:noise}
GlassNICOL assigns type automatically (we call this step the labeller): the height of a depth cluster above the table plane, measured in the capped pass, is matched to the nearest of six known glass heights (5, 9, 11, 13, 17.5 and 22\,cm) within 3\,cm, all constants of the released labelling code~\cite{gajdosech2025glassnicol}. Height is measured per view, but type is constant for the object, so one glass should carry one type across the 25 views of its scene. We register each of the \AdjFramePairs{} consecutive training frame pairs by background features, carry every box through the registration and match it to the box it overlaps in $f{+}1$ at intersection over union (IoU) above $0.5$; pairs whose registration fails are matched unwarped. Of the \FlipPairsReg{} matched pairs, \FlipCountReg{} (\FlipRateReg\%) carry a different type (Fig.~\ref{fig:noise}), \FlipNeighbourPct\% of them between glasses of neighbouring height.

The dataset authors likewise use manual test annotations to reduce noise from automatic labelling~\cite{gajdosech2025glassnicol}. Those splits yield \ManStdFlips{} flips in \ManStdPairs{} matched pairs on the standard test, at most \ManOODFlips{} in \ManOODPairs{} (\ManOODFlipRate\%) on the out-of-distribution (OOD) test. It also places two boxes on one glass in \DupFrames{} training frames, \DupDiffType{} of \DupPairs{} such pairs with different types, and none in the manual splits.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/figure_labelnoise.pdf}
  \caption{Automatic type labels are not constant on one object. (a) The same glass in adjacent training views $f$ and $f{+}1$, cropped from released frames with the released box and type. (b) Released types of the \FlipPairsReg{} box pairs matched across adjacent views after background registration, each unordered pair counted once; \FlipCountReg{} (\FlipRateReg\%) disagree, \FlipNeighbourPct\% of them between neighbouring heights. Manual test labels flip \ManStdFlips/\ManStdPairs{} and \ManOODFlips/\ManOODPairs.}
  \label{fig:noise}
\end{figure}

The misreads mostly join neighbouring heights and mostly run downward. Treating the views of one glass on an adjacent-frame chain (the tracklets of Section~\ref{sec:vote}) as repeated readings and estimating the labeller's confusion by expectation--maximisation~\cite{dawid1979em}, a water glass is read as a shorter type in \EmDownWater\% of views and as a taller one in \EmUpWater\%, a beer glass \EmDownBeer\% against \EmUpBeer\%, a wine glass \EmDownWine\% against \EmUpWine\%, a highball \EmDownHigh\%; only the shot glass, which has no shorter type, is misread upward, as whisky in \EmUpShot\%; the full estimated confusion is released with the code.


\subsection{Association through the table plane}
\label{sec:assoc}
Frames $f$ and $f{+}k$ of a scene, $k\le\SceneHops$, are registered by background features: up to 1,500 ORB features~\cite{rublee2011orb} are extracted outside the label boxes, which are dilated $1.2\times$ and masked out, matched under a $0.75$ ratio test, and a homography is fitted by RANSAC (random sample consensus)~\cite{fischler1981ransac} at a 3-pixel threshold and kept when at least 30 matches are inliers and they are at least half of the matches. The background is the table, so the homography is that plane's and carries points lying on it exactly. We take a glass's base point to be the bottom-centre of its box, an approximation the tolerance below absorbs; the rest of the box moves with parallax. The release annotates a base point per glass for the training split only, not for the hand-labelled splits where we check the association, so we read it off the box. Each box of $f$ is carried into $f{+}k$ by its base point and linked, one-to-one in order of distance, to the box whose base point lies within \SceneTol{} of the mean box width. Union-find merges links across the scene into chains, one glass seen in several views, under one constraint: a chain holds at most one box per frame, and a link that would violate it is refused (\SceneRefused{} of the links proposed by the \SceneRegPairs{} registered pairs).



\subsection{Vote, coverage and validity}
\label{sec:vote}
Each chain takes its most frequent type; ties are left unchanged. Only the type id changes: no box moves, no image is added or removed, and roster and hyperparameters match the raw-label arm of Section~\ref{sec:results}. The association runs on all 95 training scenes, of which the detector roster below uses 70: \SceneRegPairs{} of \SceneTriedPairs{} frame pairs register, and \ChainBoxes{} boxes fall into \SceneChains{} chains with a median of \SceneMedianLen{} views per box, \SceneBoxesGeThreePct\% voted on by three views or more, \SceneChanged{} (\SceneChangedPct\%) changing type and \SceneTied{} in tied chains keeping their label. The adjacent-frame overlap vote of our earlier baseline, which registered \ChainFramePairsReg{} of \AdjFramePairs{} pairs by a partial-affine warp, gives \TrkChains{} chains, \TrkSingletons{} of them a single box, a median of \TrkMedianVotes{} votes per box, and changes \TrkChanged{} boxes (\TrkChangedPct\%). Ablating the two ingredients under the homography registration (post hoc; full table with the code) separates them: overlap linking gives a median of \AblOvOneVotes{} votes over one hop and \AblOvTwoVotes{} over two, the base point over one hop \AblBaseOneVotes{} votes but \AblBaseOneOODDis{} disagreements on the OOD split, and both together \SceneMedianLen{} votes at \SceneChangedPct\% of ids changed. Three of the four settings merge no two glasses of different type on either manual split; among those three, ours votes with the most views. That baseline is the tracklet arm of Section~\ref{sec:results}. The validation split is left as released: checkpoint selection uses generic average precision (AP), so type ids never enter it.

\noindent\textbf{Validity.} Manual types are constant per object, so on the hand-labelled splits every within-chain type disagreement is a merged pair of glasses. The association above produces \ManualChainStd{} disagreeing boxes on the standard split and \ManualChainOOD{} on the OOD split, with median chain lengths of \ManualMedianStd{} and \ManualMedianOOD{} (\ManualGeThreeStd{} and \ManualGeThreeOOD{} boxes in chains of three or more). Hops and tolerance were chosen by this check, on a grid of two to four hops and four tolerances, before any scene-vote detector was trained, taking the loosest setting that disagrees nowhere; the zero above is therefore in-sample, and at the chosen tolerance three and four hops leave four disagreements on the standard split. The manual splits never enter training.

\noindent\textbf{Scope.} A vote over views removes disagreement that varies by view and inherits whatever is constant across views, such as the class-share difference of Section~\ref{sec:limits}.

\section{Experimental results}
\label{sec:results}
\subsection{Setup}
\label{sec:setup}
\noindent\textbf{Data.} The official GlassNICOL head-camera split~\cite{gajdosech2025glassnicol}: 2,375 training images from 95 scenes, 125 validation images, and hand-labelled standard and OOD tests (66 and 75 images, 3 scenes each). The roster, fixed beforehand, uses \ScenesTrain{} scenes (\ImagesTrainScene{} images).

\noindent\textbf{Detector and regime.} YOLO11n~\cite{jocher2024yolo11} (2.59M parameters~\cite{sapkota2025yolo}), $640\!\times\!640$ inputs, batch 64, AdamW~\cite{loshchilov2019adamw} at $\mathrm{lr}_0=10^{-3}$, cosine decay over \SceneEpochs{} epochs. All arms train under the same repeated-exposure regime: the \SceneRepeatSlots{} training images whose transparent and chalk-coated captures register by the same background matching appear twice, \SceneSlots{} slots and \SceneUpdates{} optimiser updates, and the arms differ only in training type ids: raw, tracklet vote, scene vote. Seeds 1337, 3407 and 4567 run on accelerator A (an H200 GPU) and seeds 1337 and 5678 on accelerator B (an RTX PRO 6000 GPU); a cell is one (accelerator, seed) set of the three arms, an arm being one label variant on the shared roster. On A the raw and scene-vote runs of a cell share a GPU and the tracklet run used another; on B all share one.

\noindent\textbf{Scoring.} The checkpoint with the best validation generic AP is scored at confidence floor $0.001$ with non-maximum suppression (NMS) at IoU $0.7$: COCO (Common Objects in Context) box AP over IoU $0.50$--$0.95$~\cite{lin2014coco}; generic AP collapses the six types to one. Within each image, GT boxes in ascending annotation-id order greedily take the unused prediction with maximum IoU ($\geq0.5$, confidence $\geq0.25$), irrespective of type. Pairwise type accuracy is the correctly typed fraction on boxes localised by both compared arms; recall is the localised fraction per arm.

\noindent\textbf{Existing methods compared.} Two standard answers to label noise are trained as arms under the same protocol. Label smoothing~\cite{muller2019labelsmoothing} keeps the released ids and smooths the classification target of the assigned anchors by $0.1$. Detector-confidence relabelling replaces a type id when a detector disagrees with confidence at least \ConfidConf, judged by a model trained on the scene-disjoint half of the roster that excludes that box, so the judge sees half the scenes at a quarter of the updates; it is the confident-learning idea~\cite{northcutt2021confident,chachula2026clod} at one global threshold, flipping rather than pruning, and changes \ConfidChangedPct\% of the ids on that roster against our \SceneChangedRosterPct\%.

\noindent\textbf{Protocol.} The comparison was preregistered before any scene-vote detector was trained: the three arms, the five cells, the checkpoint rule, the four metrics above, and four predictions, a positive mean type-accuracy gain over the raw labels on both splits (P1), an OOD gain exceeding the tracklet vote's (P2), recall and generic AP within two points (P3), and a positive OOD gain in at least 4 of 5 cells (P4). The sealed protocol, every evaluation receipt and the code are at \url{https://github.com/HeunSeungLim/glassnicol-scene-relabel}. Every contrast is read as a mean over cells with the count of positive cells beside it~\cite{bouthillier2021variance}.




\begin{table*}[t]
\centering
\caption{All five training-label variants on the official test splits (manual labels), mean over the \SvCells{} (accelerator, seed) cells; $\Delta$ is the change from the released labels before rounding, with the number of cells above them. Six: six-class AP; Gen: generic AP, the six types collapsed to one; Rec: localisation recall; Type: type accuracy. Every row trains the same detector on the same roster, budget and seeds, three cells on accelerator A and two on B, and the rows differ only in the training type ids, or, for label smoothing, in the classification target. Type is scored on the boxes all five arms localise in a cell (\TblCommonStd--\TblCommonStdMax{} of \TblBoxesStd{} on the standard split, \TblCommonOOD--\TblCommonOODMax{} of \TblBoxesOOD{} out of distribution), one denominator for the five columns; the preregistered predictions of Section~\ref{sec:results} use pairwise common sets instead and do not have to agree with it. The two external rows are post hoc, added after the sealed protocol. Best per column bold, second \second{blue}.}
\label{tab:main}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{0.96}

\begin{tabular}{lrrrrrrrrrr}
\toprule
& \multicolumn{5}{c}{Standard} & \multicolumn{5}{c}{OOD} \\
\cmidrule(lr){2-6}\cmidrule(lr){7-11}
Training labels & Six & $\Delta$ & Gen & Rec & Type & Six & $\Delta$ & Gen & Rec & Type \\
\midrule
\inputrows{master_v18c_hl_rows.tex}\bottomrule
\end{tabular}
\end{table*}

\subsection{What the labels change}
Table~\ref{tab:main} pools the cells; the per-cell table with its intervals is released with the code. Two contrasts hold across cells. Out of distribution, six-class AP rises by \SvSixOODVsRawGain{} points over the raw labels in \SvSixOODVsRawPos{} of \SvCells{} cells (across-cell 95\% $t$-interval \SvSixOODVsRawCI) and by \SvSixOODVsTrkGain{} over the tracklet vote (\SvSixOODVsTrkCI), which the tracklet vote itself did not move (\SvSixOODTrkVsRawGain). On the standard split localisation recall falls in every cell, by 2.0 points on average (\SvRecStdVsRawCI). The type-accuracy gains are larger but less certain: out of distribution the scene vote gains \SvTypeOODVsRawGain{} points over the raw labels, positive in \SvTypeOODVsRawPos{} of \SvCells{} cells, with per-cell paired bootstrap intervals over \SvBootDraws{} draws that resample test images above zero in \SvOODAbove{} cells, reaching it in \SvOODStraddle{} and below it in \SvOODBelow{}; resampling whole scenes instead leaves \SceneBootZero{} of the \SceneBootCells{} per-cell intervals over both splits including zero, and the across-cell interval is \SvTypeOODVsRawCI; over the tracklet vote it gains \SvTypeOODVsTrkGain{} (\SvTypeOODVsTrkPos{} of \SvCells). On the standard split type accuracy changes by \SvTypeStdVsRawGain{} over the raw labels (\SvTypeStdVsRawPos{} of \SvCells{} cells positive) and \SvTypeStdVsTrkGain{} over the tracklet vote (\SvTypeStdVsTrkPos{}), while six-class and generic AP change by \SvSixStdVsRawGain{} and \SvGenStdVsRawGain{} (\SvSixStdVsRawPos{} and \SvGenStdVsRawPos{} of \SvCells{} positive).

\noindent\textbf{Preregistered predictions.} P1 held: the mean type-accuracy gain over the raw labels is positive on both splits, \SvTypeStdVsRawGain{} and \SvTypeOODVsRawGain. P2 held: out of distribution the gain exceeds the tracklet vote's \SvTypeOODTrkVsRawGain{} (pairwise contrasts use different common-box sets and need not add). P4 held: \SvTypeOODVsRawPos{} of \SvCells{} OOD cells are positive. P3 did not: standard-split recall falls 2.0 points, just past the bound, and generic AP 1.7; out of distribution both stay within it. Scoring the last epoch instead (post hoc) gives type accuracy \SvLTypeStdVsRawGainL{} on the standard split, so P1 holds only under the preregistered rule, and \SvLTypeOODVsRawGainL{} out of distribution; recall moves \SvLRecStdVsRawGainL{} and \SvLRecOODVsRawGainL, so the OOD gain holds under both rules while the standard-split recall loss does not. Under that rule the tracklet vote also gains out of distribution, six-class AP \TrkLSixOODVsRawGainL{} in \TrkLSixOODVsRawPosL{} of \SvLCells{} cells, but the scene vote still leads it by \SvLSixOODVsTrkGainL{} points in \SvLSixOODVsTrkPosL{} of \SvLCells{} (\SvLSixOODVsTrkCI, against \SvSixOODVsTrkCI{} under the preregistered rule): the lead survives the rule change, but its interval now includes zero.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/figure_qualitative.png}
\caption{The five training-label arms on OOD test frames, accelerator A seed 1337, from the frozen predictions behind Table~\ref{tab:main}. Columns: the manual annotation, then the detectors trained on released labels, label smoothing, detector-confidence relabelling, the adjacent-frame vote and ours. Boxes sit at the manual location, coloured by the predicted type, solid if right, dashed with a cross if wrong, grey dotted if the arm localises nothing there. Headings score all arms on the \QualCommonBoxes{} boxes every arm localises, one shared denominator; a panel count is correctly typed boxes of the frame's manual boxes, so one no arm finds counts against all. Row 1 is a frame where the released-label arm gets the most boxes wrong, mistyped or missed (four tie; first by file name); row 2 is our worst frame, the unique maximum of boxes we get wrong, again mistyped or missed, that it gets right.}
\label{fig:qual}
\end{figure*}

\subsection{Comparison with existing methods}
\label{sec:pairs}
Table~\ref{tab:main} sets the two standard answers to label noise, and our earlier vote, beside ours; Fig.~\ref{fig:qual} shows the same five detectors on manual test frames. Label smoothing leaves the detector where the released labels leave it (\BaseLsOODGain{} out of distribution): absorbing the noise does not recover a wrong type id. Detector confidence moves the labels the most and the scores furthest the wrong way, \BaseCfOODGain{} out of distribution and above the released labels in \BaseCfOODPos{} of \SvCells{} cells. It rewrites \ConfidChangedPct\% of the ids against our \SceneChangedRosterPct\%, and \ConfidVsSceneDisagreePct\% of those differ from the type the geometry votes for, of which \ConfidFloorPct{} points are forced by the edit counts alone: a box it moves and we leave alone differs by construction. Under the preregistered rule the scene vote alone gains out of distribution in every cell, \BaseSvOODGain{} points in \BaseSvOODPos{} of \SvCells, against \BaseTrkOODGain{} in \BaseTrkOODPos{} for the adjacent-frame vote. At threshold \ConfidTauHigh{} (post hoc, accelerator A) it edits \ConfidTauHighChangedPct\% of the ids, about as many as our vote, and still falls \ConfidTauHighOODDrop{} points out of distribution in all \ConfidTauHighCells{} cells while beating us on the standard split: fewer edits soften the loss without turning it into a gain. Smoothing is the better choice on the standard split, ahead of us in every cell, so the case for relabelling is the out-of-distribution one. The last epoch keeps the order: \LastLsOODGain{} for smoothing, \LastCfOODGain{} for detector confidence, \TrkLSixOODVsRawGainL{} for the tracklet vote and \LastSvOODGain{} for ours in \LastSvOODPos{} of \SvCells.

\section{Discussion and limitations}
\label{sec:limits}
The vote does not bring class shares closer to the tests: the shot:whisky ratio is \RatioRawTrain{} in raw labels and \RatioSceneTrain{} after it, against \RatioManualStd{} and \RatioManualOOD{} in the tests; decoding the chains under the estimated confusion moves it no closer, and separating the labeller's contribution from the staging's needs the per-box height the release omits.

Six-class AP is the mean over the six types, so the standard-split \SvSixStdVsRawGain{} is a reallocation that nets out rather than a uniform decline: shot gains \ClsStdShot{} and beer \ClsStdBeer{} against \ClsStdWhisky{} for whisky and \ClsStdWater{} for water, while type accuracy on that split rises \SvTypeStdVsRawGain. Out of distribution the reallocation runs one way, \ClsOODWater{} on water and \ClsOODWine{} on wine, with no type below \ClsOODShot. The edits are short and mostly upward: \VoteOneStepPct\% of the \VoteChangedBoxes{} changed ids move a single step on the height ladder and \VoteUpPct\% move up. The shortest type can only move up and the tallest only down; over the \VoteInnerEdits{} edits that start at neither, \VoteInnerUpPct\% still move up, undoing the downward bias the labeller's estimated confusion shows.

The association is only as good as the table: frames without enough background features stay unlinked, and the \DupFrames{} frames with two boxes on one glass are counted twice.

Seed 4567 on A loses type accuracy on both splits, by eight and ten points, while every other cell gains six or more out of distribution, and the type-accuracy mean sits inside the seed spread, so six-class AP carries the claim; the tracklet contrast rerun on B for seed 1337 moved by \XHostTypeStd/\XHostTypeOOD{} type-accuracy points, a caution for single-cell results. Checkpoint selection by validation generic AP ($r^2=\SelRsq$ with test six-class AP) is shared across arms; only labels change.

\section{Conclusion}
A released benchmark can disagree with itself, and the disagreement is measurable without a model: GlassNICOL's automatic types differ on the same glass in \FlipRateReg\% of adjacent-view pairs, where its hand labels almost never do. The geometry that produced them also repairs them: a scene is static, so a homography of the table plane carries base points between views, union-find chains them, and a plurality settles the type. Only \SceneChangedPct\% of ids move, and no box does. The repair pays out of distribution, where under the preregistered rule ours alone gains in every cell, \SvSixOODVsRawGain{} six-class AP, while smoothing leaves the detector where it was and a detector-written rewrite moves it \BaseCfOODGain. Where a pipeline leaves a redundancy unused, that redundancy is a repair.

\bibliographystyle{IEEEbib}
\bibliography{references}
\end{document}
